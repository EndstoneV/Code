#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define f(i, j, k) for(int i = j; i <= k; i++)

int main() {
    double x, y;
    cin >> x >> y;
    double div = x / y;
    cout << floor(div);
    return 0;
}