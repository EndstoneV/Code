#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    // vector<int> asfs;
    // map<int, int> sfapsf;
    // list<int> sfasdfsaf;
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    while(t--) {
        int x, y, n;
        cin >> x >> y >> n;
        
    }
}