#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define f(i, j, k) for(int i = j; i <= k; i++)

int main(){
    int T, n, x;
    scanf("%d", &T);
    while(T--){
        scanf("%d%d", &n, &x);
    }
    return 0;
}