#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define f(i, j, k) for(int i = j; i <= k; i++)

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int r;
    for(int i = 1; i <= r; i++)
        for(int j = 1; j <= i; j++)
            cin >> a[i][j];
    
    return 0;
}